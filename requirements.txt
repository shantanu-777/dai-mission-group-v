# Core scientific stack
numpy>=2.1.0,<3
pandas>=2.2.0,<3
matplotlib>=3.8.0,<4
scikit-learn>=1.4.0,<2
scipy>=1.12.0,<2
seaborn>=0.13.0,<0.14

# Wilson confidence intervals for the cluster default-rate analysis
statsmodels>=0.14.0

# Excel (.xls) reading for the UCI credit-card dataset
xlrd>=2.0.1

# Unsupervised: ordinal-aware clustering (K-Modes)
kmodes==0.12.2

# Imbalance handling (SMOTE) for the supervised block
imbalanced-learn>=0.12.0

# Model explainability (feature importance) for the supervised block
shap>=0.44

# Causal inference + DAG visualisation
dowhy>=0.11.1
networkx>=3.3

# Notebook execution
jupyter>=1.1.0
nbconvert>=7.16.0
ipykernel>=6.29.0
